import React from 'react';
import Card from '../UI/Card';
import './style.css';

const Hero = props => {
    return (
        <div>
            <Card>
                Jhonata Tenorio
            </Card>
        </div>
    );
};

export default Hero;